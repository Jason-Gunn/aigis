# Documentation

This directory contains repository-only material that supports development and maintenance of the plugin.

## Layout

- `development/` — implementation journal, debugging notes, and maintenance history
- `reference/` — reference assets and exported materials
- `specifications/` — project requirements, architecture notes, and planning documents

Runtime plugin code remains under `admin/` and `includes/`.
